[package]
name = "printr"
version = "0.1.0"
edition = "2024"

[lib]
name = "printr"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
rayon = "1.8"
crossbeam = "0.8"

[profile.release]
lto = true
codegen-units = 1
